--- src/services/overview_service.rs
+++ src/services/overview_service.rs
@@ -1157,10 +1157,15 @@ impl OverviewService {
         
         // Module 17: Capacity Prediction (P2)
-        let capacity = self.predict_capacity(cluster_id).await?;
-        let real_data_size_bytes = self.get_real_data_size(cluster_id).await?;
-        if let Some(ref mut cap) = capacity {
-            cap.real_data_size_bytes = real_data_size_bytes;
+        let mut capacity = match self.predict_capacity(cluster_id).await {
+            Ok(cap) => Some(cap),
+            Err(e) => {
+                tracing::warn!("Failed to predict capacity for cluster {}: {}", cluster_id, e);
+                None
+            }
+        };
+        if let (Some(cap), Some(stats)) = (&mut capacity, &data_stats) {
+            cap.real_data_size_bytes = stats.total_data_size;
         }
         
         // Module 18: Alerts (P2)
 
